jenkins:
  systemMessage: "\n\nDemo setup for Jenkins Configuration as Code plugin......\n\n"

  numExecutors: 1
  mode: NORMAL
  scmCheckoutRetryCount: 3
  labelString: "master"  

  securityRealm:
    local:
      allowsSignup: false
      users:
       - id: admin
         password: ${adminpw:-passw0rd}

  authorizationStrategy:
    globalMatrix:
      permissions:
        - "Overall/Read:anonymous"
        - "Job/Read:anonymous"
        - "View/Read:anonymous"
        - "Overall/Administer:anonymous"

  crumbIssuer: "standard"

  remotingSecurity:
    enabled: true    

credentials:
  system:
    domainCredentials:
      - credentials:
        - usernamePassword:
            scope:    GLOBAL
            id:       github-user
            username: shahshadab1680
            password: ${github:-githubPassw0rd}
            description: github username/password  

tool:
  git:
    installations:
      - name: Default
        home: "git"

credentials:
 system:
   domainCredentials:
     - credentials:
         - string:
             scope: GLOBAL
             id: slack-token
             description: "Slack access token"
             secret: xoxb-497707237954-3873865554562-uv1Snl4AgI2n2Ldoj48gSikN

unclassified:
 slackNotifier:
   teamDomain: eurustechnologies.slack.com
   tokenCredentialId: "slack-token"
   botUser: true
   


